# Template pour secrets Streamlit. À copier en .streamlit/secrets.toml (gitignored)
# et adapter localement. En prod (Streamlit Cloud), définir dans le dashboard
# "Settings -> Secrets" en TOML format.

# Mot de passe partagé requis pour accéder à l'app.
# Choisis quelque chose de simple à partager sur Discord mais non trivial.
password = "change-moi"
